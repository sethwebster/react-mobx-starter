import React, { Component } from 'react';
import { observer } from 'mobx-react';

/* eslint-disable */
@observer
class App extends Component {
  state = {
  }

  render() {
    return <div>MobX React Starter</div>
  }
}

export default App;
